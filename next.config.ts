import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Vercel handles SSR and image optimization natively.
  // Add `output: 'export'` if deploying to Cloudflare Pages instead.
};

export default nextConfig;
